extends Node

# Dummy script to prevent errors since the user removed the real VFX manager.
# Contains an empty spawn function so existing code doesn't crash if called.

func spawn(effect_name: String, pos: Vector3):
	pass
